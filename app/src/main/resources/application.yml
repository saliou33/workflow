spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wkflow?useSSL=false
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

idm:
  jwtSecret: aeee730baff4b270b49ae582ed92e4d300fa2e39cd385b516ed
  jwtExpirationMs: 86400000

service:
  message: Hello World

server:
  port: 8083
